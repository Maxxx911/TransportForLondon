import React from "react";

class BusTable extends React.Component{
	render(){
		return(

            <div class="routes-block">
              <a> Взять через цикл все значения num в api </a>
              <a> Взять через цикл все значения num в api </a>
              <a> Взять через цикл все значения num в api </a>
              <a> Взять через цикл все значения num в api </a>
            </div>

		);
	}
}

export default BusTable;
